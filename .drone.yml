kind: pipeline
type: ssh
name: deploy to VM

server:
  host: 192.168.56.1
  user: test
  password:
    from_secret: ssh_psw

steps:
- name: deploy
  commands:
  - cd release
  - ./build.sh
  - ./utils.sh

trigger:
  branch:
  - main
  - drone-deploy
  event:
  - push
  - tag